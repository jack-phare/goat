# Copy to dev/.env and fill in the keys you have.
# You only need ONE provider — models without keys will be unavailable.

# LiteLLM master key (default: sk-dev-key)
# LITELLM_MASTER_KEY=sk-dev-key

# Azure OpenAI (primary provider for gpt-4o-mini, gpt-5-nano, gpt-5-mini)
AZURE_API_KEY=
AZURE_API_BASE=

# Groq (free tier: https://console.groq.com)
GROQ_API_KEY=

# OpenAI (direct — for embeddings or direct chat)
OPENAI_API_KEY=

# Anthropic (uncomment in litellm-config.yaml to enable)
ANTHROPIC_API_KEY=

# Langfuse: no config needed — runs locally in docker-compose.
# UI: http://localhost:3001 (admin@goat.local / admin)
# API keys are hardcoded: pk-lf-goat-dev / sk-lf-goat-dev
